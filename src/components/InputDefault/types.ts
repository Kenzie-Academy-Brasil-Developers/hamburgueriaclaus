export interface iInputDefaultPros {
    id: string;
    label: string;
    type: string;
    status: string;
    error?: string;
}

export interface iStyledProps {
    currColor: string;
    currValue: string;
}