export interface iInputDefaultPros {
    id: string;
    placeholder: string;
    label: string;
    type: string;
    status: string;
    error?: string;
}

export interface iStyledProps {
    currColor: string;
}